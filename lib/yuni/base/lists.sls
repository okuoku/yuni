;; (yuni base lists) R6RS compat list utilities
(library (yuni base lists)
         (export
           find
           for-all
           exists
           filter
           partition
           fold-left
           fold-right
           ;; remp remove remv remq ;; FIXME: Use SRFI-1 instead?
           cons*
           )
         (import (rnrs))

;; FIXME: Implement it!
         
)
