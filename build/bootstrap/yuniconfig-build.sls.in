(library (yuniconfig build)
         (export yuniconfig-platform)
         (import (yuni scheme))

;;

(define (yuniconfig-runtime-rootpath) "@YUNIRUNTIMEROOTPATH@")
(define (yuniconfig-platform) "@YUNIPLATFORM@")
)
